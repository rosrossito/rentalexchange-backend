################### DataSource Configuration ##########################

spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/rental
spring.datasource.username=postgres
spring.datasource.password=qa1234
spring.jpa.show-sql = true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.log-validation-errors=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.default_schema=rental


#jdbc.driverClassName=org.postgresql.Driver
#jdbc.url=jdbc:postgresql://localhost:5432/rental
#jdbc.username=postgres
#jdbc.password=qa1234

#init-db=true

################### Hibernate Configuration ##########################

#hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#hibernate.show_sql=true
#hibernate.hbm2ddl.auto=update

################### JavaMail Configuration ##########################
smtp.host=smtp.gmail.com
smtp.port=587
smtp.protocol=smtp
smtp.username=exchange.rental.info
smtp.password=exchange.rental.info1
smtp.auth=true
smtp.starttls.enable=true
smtp.debug=true

#support.email=rentalexchange.t@gmail.com

# Level for loggers on classes inside the root package "rentalecchange-backend" (and its
# sub-packages)
# Available levels are: TRACE, DEBUG, INFO, WARN, ERROR, FATAL, OFF
logging.level.rentalecchange-backend = DEBUG

# Log file location (in addition to the console)
logging.file = C://logs/application.log

#Period of activation link life in seconds
activation.link.period=30


